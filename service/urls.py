# -*- coding: utf-8 -*-

from django.conf.urls import patterns, url


urlpatterns = patterns('',
                       # Examples:
                       url(r'^$', 'search_platform.views.home', name='home'),
                       # url(r'^blog/', include('blog.urls')),
                       # *url_pattern_list,

)
